# Vibe Editor

Векторный графический редактор, построенный с использованием Vue 3, TypeScript и HTML5 Canvas API. Позволяет создавать и редактировать простые векторные фигуры (круги, прямоугольники, линии, кривые Безье).

## 🎯 Описание Проекта

**Vibe Editor** — минималистичный векторный редактор без внешних библиотек для рендера. Приложение ориентировано на простоту архитектуры, производительность и легкость расширения.

### Основные Возможности (MVP)

- ✅ Создание фигур: круги/овалы, прямоугольники, линии
- ✅ Выбор и перемещение фигур
- ✅ Редактирование свойств (цвет, толщина обводки, размер)
- ✅ Удаление фигур
- ✅ Быстрый интерфейс с Toolbar и Properties Panel
- ✅ Базовая рендеризация Безье кривых (без редактирования контрольных точек)

### Планируется (Этап 3)

- 🔄 Масштабирование и панорамирование холста
- 🔄 Resize и rotate фигур с handles
- 🔄 Undo/redo история
- 🔄 Редактирование контрольных точек Безье
- 🔄 Floating properties panel
- 🔄 Экспорт/импорт (JSON, SVG)
- 🔄 Слои и группировка

---

## 🛠️ Технологический Стек

- **Frontend Framework:** Vue 3 (Composition API)
- **Language:** TypeScript 5.9+
- **Build Tool:** Vite 7.3+
- **State Management:** Pinia 3.0+
- **Canvas Rendering:** Чистый HTML5 Canvas API (без внешних библиотек)
- **Code Quality:** ESLint, Prettier, OxLint
- **Node Version:** >=20.19.0

---

## 📁 Структура Проекта

```
src/
├── canvas/                        # Модуль холста (рендер + взаимодействия)
│   ├── components/
│   │   └── VectorCanvas.vue      # Главный canvas компонент
│   ├── composables/
│   │   ├── useCanvasRender.ts    # Логика рендеринга фигур
│   │   ├── useInteractions.ts    # Обработка мыши (выбор, drag)
│   │   └── useShapes.ts          # Управление фигурами
│   ├── types/
│   │   └── shape.ts              # Интерфейсы Shape, Viewport, и т.д.
│   └── utils/
│       └── math.ts               # Утилиты (bounding box, hitTest, etc.)
│
├── gui/                           # Модуль интерфейса
│   ├── components/
│   │   ├── EditorToolbar.vue     # Панель инструментов
│   │   └── PropertiesPanel.vue   # Панель свойств фигур
│   └── composables/
│       └── useTools.ts           # Управление активным инструментом
│
├── stores/                        # Pinia stores (состояние приложения)
│   ├── canvas.ts                 # Store для фигур и viewport
│   └── tools.ts                  # Store для активного инструмента
│
├── shared/                        # Общие утилиты (если потребуются)
│
├── App.vue                        # Корневой компонент (главный layout)
└── main.ts                        # Точка входа приложения
```

### Принципы Архитектуры

1. **Разделение слоев:** Canvas модуль отвечает за рендер и низкоуровневые взаимодействия, GUI модуль — за интерфейс пользователя
2. **Единое состояние:** Все изменения проходят через Pinia stores
3. **Типизация:** Полная типизация через TypeScript
4. **Модульность:** Каждый composable отвечает за одну область функционала
5. **Расширяемость:** Новые типы фигур добавляются без изменения архитектуры

---

## 🚀 Быстрый Старт

### Требования

- Node.js >=20.19.0 или >=22.12.0
- npm или yarn

### Установка

```bash
# Клонировать репозиторий
git clone <repo-url>
cd vibe-editor

# Установить зависимости
npm install
```

### Разработка

```bash
# Запустить dev server
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173` (или другому, указанному в консоли).

### Production Build

```bash
# Type-check + минификация
npm run build

# Preview собранного приложения локально
npm run preview
```

### Качество Кода

```bash
# Запустить ESLint + Prettier форматирование
npm run lint

# Форматировать код
npm run format

# Type-check без сборки
npm run type-check
```
